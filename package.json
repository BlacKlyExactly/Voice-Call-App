{
  "name": "voice-call-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "startDev": "concurrently \"cd server && nodemon index.ts\" \"cd client && yarn start\"",
    "start":  "concurrently \"cd server && yarn start\" \"cd client && yarn start\"",
    "install": "concurrently \"cd server && yarn install\" \"cd client && yarn install\"",
    "build": "cd client && react-scripts build",
    "test": "cd client && react-scripts test",
    "eject": "cd client && react-scripts eject"
  },
  "devDependencies": {
    "concurrently": "^6.0.0"
  },
  "dependencies": {
    "peerjs": "^1.3.1"
  }
}
